# -*- coding: utf-8 -*-
"""
Created on Thu Aug 29 00:06:32 2019

@author: Enrique Buendia
"""

from os import path

script_dir = path.dirname(__file__) #<-- absolute dir the script is in

espectro = input('Nombre del archivo: ')
abs_file_path = path.join(script_dir, 'espectro_txt\\' + espectro)
txt = open(abs_file_path,'r')
espectro_txt = pd.read_csv(txt,header=None,delim_whitespace=True)
txt.close()

flujo = espectro_txt.iloc[:,1];flujo = flujo.to_numpy() ### se carga el flujo
LO = espectro_txt.iloc[:,0];LO = LO.to_numpy() ### se carga la longitud de onda
